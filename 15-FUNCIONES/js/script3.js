//Parametros y retornos

console.warn("Inicio programa");

let one = prompt("Ingrese el nombre de un NPC");

function npc(name) {
  return name;
}

console.log(npc(one));
alert(npc(one));
let name = npc(one);
